[package]
name = "bash-helper"
version = "0.1.0"
edition = "2021"

[dependencies]
gethostname = "1.1.0"

[profile.release]
opt-level = 3
lto = true
strip = true
panic = "abort"
codegen-units = 1  # Reduce codegen units for better optimization

