[package]
name = "filestile"
version = "0.3.0"
edition = "2024"

[dependencies]
chrono = "0.4.31"
lexopt = "0.3.1"
regex = "1"

[profile.release]
opt-level = 3
lto = true
strip = true
panic = "abort"
codegen-units = 1  # Reduce codegen units for better optimization

